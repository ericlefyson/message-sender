# Repository Mode
# Options: "mock" or "api"
# - mock: Uses local mock data (no backend connection required)
# - api: Connects to real backend API
VITE_REPOSITORY_MODE=api

# API Configuration (only needed when VITE_REPOSITORY_MODE=api)
# Backend REST API URL
# Local: http://localhost:3001
# Production: https://your-backend.railway.app or your backend domain
VITE_API_URL=http://localhost:3001

# WebSocket URL (only needed when VITE_REPOSITORY_MODE=api)
# Backend WebSocket URL
# Local: ws://localhost:3001
# Production: wss://your-backend.railway.app (MUST use wss:// for HTTPS deployments)
VITE_WS_URL=ws://localhost:3001
